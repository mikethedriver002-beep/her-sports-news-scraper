name: HSD Legacy Results Source Audit - Manual Quarantined

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  manual-readonly:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Legacy quarantine notice
        run: |
          echo "This legacy source-audit workflow is quarantined."
          echo "It no longer runs on schedule and does not reference paid-source secrets."
          echo "Use the HSD V3 Repo State Sanity workflow for free-only source diagnostics."
