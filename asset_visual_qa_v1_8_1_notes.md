# Asset Visual QA v1.8.1

Small event-date bridge patch.

## Key changes

- Adds `generate_hsd_visual_upgrade_v2.py` v2.6 with event date propagation.
- Copies bundle event dates into `studio_render_manifest_v2.json`.
- Copies bundle event dates into render manifest `source_facts`.
- Cleans up graphics QA issue wording so freshness blocks are no longer called missing assets.
