# HSD Player Registry

Mermaid's player graphics system is now registry-first.

Put operator-approved player images here and list them in `player_registry.csv`.

The registry rule is:
- exact player identity
- exact current team
- known source
- rights status
- active usage status

No player graphic should be generated from a name alone.
